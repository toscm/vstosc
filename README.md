# toscs-vscode-commands

Private VSCode commands of tosc.

## Commands

*KnitRmd*: sends the following text to the currently active console:

```R
if (exists("params")) rm(params)
rmarkdown::render("<path-of-currently-active-file")
```
# toscvs
